form: {
location: [Pin]
reason: String
user: String
}
<article id="form">
    {{!-- TODO: SEND THIS VARIABLE --}}
    <h1>Add a recommendation for {{place}}</h1>
    {{!-- TODO: add createRec endpoint --}}
    <form id="createForm" method="POST" action=/createRec>
        <div class="input-field">
            <label>Recommendation location:</label><br>
            {{!-- Populate with dropdownlist of names from database --}}
            <script>
                Movie.find({}, function(err, movies) {
                    if (err) throw err;
                    //add, using jquery, the options
                    // <option value=""></option>
                });
            </script>
            <select id="recLocation"></select>
        </div>
        <div id="space"></div>
        <div class="input-field">
            <label>Your name:</label><br>
            <input type="text" name="author" size="50" placeholder="ex: Tiff">
        </div>
        <div id="space"></div>
        <div class="input-field">
            <label>Reason for reccomending this location:</label><br>
            <input type="text" name="reason" size="50" placeholder="ex: this place also makes great coffee!">
        </div>
        <div id="space"></div>
        <div id="space"></div>
        <button id="submitPin" type="submit">Submit!</button>
    </form>
</article>